# Required for discovery runs only. Replay never calls the model.
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4o

# Target app (the local legacy stand-in).
MERIDIAN_PORT=4600

# Mock operator console.
OPERATOR_PORT=4610

# Demo credentials for the local stand-in app. These are fake by construction;
# they are classified as secrets in policy and never reach artifacts or logs.
MERIDIAN_USERNAME=teller01
MERIDIAN_PASSWORD=demo-pass-2024
